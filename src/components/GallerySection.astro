---
import GalleryLightbox from './GalleryLightbox.vue';

const { items, subtitle } = Astro.props;
const count = items?.length || 0;
---

{items.length > 0 && (
  <section id="galeri" class="galeri">
    <div class="container">
      <div class="galeri-head" data-reveal>
        <div>
          <p class="eyebrow">Galeri Progress · {count} foto</p>
          <h2>Lihat progres<br/><em class="accent-serif">pembangunan</em> terkini</h2>
        </div>
        <p class="galeri-note">{subtitle || 'Foto progress pembangunan langsung dari lapangan — otomatis bertambah saat foto baru ditambahkan di sheet Gallery kategori progress.'}</p>
      </div>
      <div data-reveal style="--reveal-delay: 120ms">
        <GalleryLightbox items={items} client:visible />
      </div>
    </div>
  </section>
)}

<style>
  .galeri {
    background: var(--color-bg-main);
    padding-block: clamp(56px, 8vw, 110px);
  }
  .galeri-head {
    display: flex;
    flex-direction: column;
    gap: 14px;
    margin-bottom: clamp(28px, 4vw, 44px);
  }
  .galeri-head h2 { margin-top: 14px; font-size: clamp(1.8rem, 3.4vw, 2.7rem); }
  .galeri-note {
    max-width: 340px;
    font-size: 0.85rem;
    color: var(--color-muted);
  }
  @media (min-width: 860px) {
    .galeri-head { flex-direction: row; justify-content: space-between; align-items: flex-end; }
  }
</style>
